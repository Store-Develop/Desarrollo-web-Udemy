// Objeto Math

resultado = Math.PI;
resultado = Math.round(2.5);
resultado = Math.ceil(2.2); // ceil redondea hacia arriba
resultado = Math.floor(2.9); // floor redondea hacia abajo
resultado = Math.sqrt(144);
resultado = Math.abs(-200); // combierte un número negativo en uno positivo
resultado = Math.min(3, 5, 1, 8, 2, 10); // nos muestra el número menor en un listado
resultado = Math.max(3, 5, 1, 8, 2, 10); // nos muestra el número mayor en un listado
resultado = Math.random();
resultado = Math.random(Math.floor(Math.ramdon() * 30));

console.log(resultado);

